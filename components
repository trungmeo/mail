
export default function Navbar() {
  return (
    <nav className="w-full px-8 py-5 flex justify-between items-center bg-black bg-opacity-70 shadow-lg fixed top-0 z-50">
      <div className="flex items-center space-x-3">
        <div className="w-12 h-12 flex items-center justify-center rounded-full bg-neon-blue shadow-neon font-bold text-2xl text-black select-none border-2 border-white">TL</div>
        <div className="text-lg text-neon-blue font-semibold tracking-wider">| Tool Checker Siêu Tốc</div>
      </div>
      <div className="space-x-8 text-lg">
        <a href="/" className="hover:text-neon-blue transition">Home</a>
        <a href="/history" className="hover:text-neon-blue transition">Lịch sử</a>
        <a href="/api-docs" className="hover:text-neon-blue transition">API Docs</a>
      </div>
    </nav>
  );
}
